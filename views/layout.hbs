<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{#each css}}
    <link href="{{this}}" rel="stylesheet" type="text/css">
    {{/each}}
  </head>
  <body>
    {{{body}}}
    {{#each scripts}}
    <script src="{{this}}"></script>
	{{/each}}
  </body>
</html>
